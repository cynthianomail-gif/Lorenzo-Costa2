# 規則 v4：五色角色列 ＋ FG 航海地圖

日期：2026-08-14
狀態：已實作（`RULE_VERSION = 4`，並設為預設版本）
來源：企劃 2026-08-13 口述的 v4 改動說明（NG／FREE GAME 兩段）。
※ v1／v2／v3 完全保留，可在調校面板「🗺️ 規則」分頁切回對照。

## 與 v3 的差異總表

| 項目 | v3 | v4 |
|---|---|---|
| 一般圖示 | 8 個（4 色 × 角色／武器） | **10 個**（新增紫色：阿塔蘭忒／弓） |
| 分裂來源 | 地圖通關 | **合成本身**：同色合成切一刀，進階合成切第二刀 |
| 分裂上限 | NG／FG 2×1、SFG 4×1 | **一律 3×1**（一刀＋進階第二刀） |
| NG 地圖 | 單線四關＋盤面倍數 | **整段移除**（無地圖、無倍數） |
| JP 條件 | 雙神各湊 2 組顏色組合 | **角色列**：一整列 5 隻角色相鄰，中間格定獎 |
| 彩金 | 無（只有兩尊神 JP） | **五階**：宙斯 JP／赫拉 JP／MAJOR／MINOR／MINI |
| FG 地圖 | 沿用 NG 的關卡地圖 | **新的航海地圖**：連爆推進，倍數階梯 |

## 一、圖示：新增第 5 色

`SYMBOLS` 由 10 筆變 12 筆。**id 配置有動**：

| id | 內容 |
|---|---|
| 0–7 | 紅／金／藍／綠 各 角色＋武器（不變） |
| **8, 9** | **阿塔蘭忒（char）／弓箭（weapon），綠 `#059669`，colorId 4** |
| **10, 11** | 船頭／船尾（原本是 8／9，**往後移了**） |

這樣排是為了保住 `colorId = Math.floor(id / 2)`、`id % 2 === 0 ⇒ 角色` 這兩條全檔到處在用的約定。
代價是原本寫死的 `id === 8`／`id === 9`／`id < 8` 全部要換掉，改用：

- `SC_BOW` / `SC_STERN`（＝10／11）
- `isScSymId(id)`、`isNormalSym(id)`（id 0..9）、`isCharSym(id)`
- `NORMAL_SYM_COUNT`（＝10）、`COLOR_COUNT`（＝5）、`newSplitArray()`

**踩過的坑**：第一輪掃描用 `id === 8` 之類的字面比對，漏掉了 `applyGravity()` 裡變數名為 `_gId` 的那一組
（`_gId >= 8`）。補牌路徑漏改的話，紫色圖示會被當成船頭船尾。改這類全域邊界時，
搜尋要用「`>= 8`／`=== 8`」而不是「`id >= 8`」。

**紫色只在 v4 出現**：靠 `activeColorCount()`（v4 回 5、其餘回 4）在 `symWeights()` 裡把
未啟用的色權重補 0。v1～v3 因此完全抽不到 id 8／9，四色行為原封不動。

## 二、分裂（刀）：一刀 ＋ 進階第二刀

| 事件 | 效果 |
|---|---|
| 同色（角色＋武器）相鄰合成 | 該色 `splitCount = 2`（切一刀）。**再合成幾次都還是 2** |
| 兩個相鄰 1×2 不同色 → 進階合成 1×4 | 兩色各 `splitCount = 3`（第二刀，上限） |
| 角色列 1×5 | **不給分裂**（企劃 2026-08-14 確認：純粹給分／發獎，不算一次合成） |

`V4_SPLIT_BASE = 2` / `V4_SPLIT_ADV = 3`，`maxSplitCap()` 在 v4 回 3。

上限壓在 3 是照企劃的終態數字回推的：五色全切滿時
`ways = (ROWS × 3)^COLS = 12^5 = 248,832`，與企劃書一致（放到 5 刀會變成 3,200,000）。
單元測試 `ways.248832` 就是在守這個數字。

保留規則沿用舊制：**NG 每局歸零，FG／SFG 內跨局保留，FG／SFG 結束才一併重置**。

## 三、取得 JP／彩金：角色列

**條件**：任一橫列的 5 格全是「未合成的角色圖示」，且 5 隻**各不相同**
（盤面 5 欄、v4 剛好 5 色 ⇒ 等於五隻角色排滿一整列）。

**定獎**：看該列**中間格（第 3 欄，`V4_CENTER_COL = 2`）**是誰。

| 中間格 | 獎項 |
|---|---|
| 海克力斯 | ⚡ 宙斯 JP |
| 伊阿宋 | 👑 赫拉 JP |
| 美狄亞 | MAJOR（100 × bet） |
| 奧菲斯 | MINOR（25 × bet） |
| 阿塔蘭忒 | MINI（10 × bet） |

**結算**：該整列合成為一個 **1×5 合成格**（`isHeroRow`）。

> **1×5 不是 wild**（企劃 2026-08-14 確認）。合成後它就是**一個全新的圖示**：
> 不替代任何顏色、不與任何符號連線、不進 Ways、不給分。
> 「給分」指的是 JP／MINI／MINOR／MAJOR 這個獎項本身，不是靠這一列去湊連線。

獎項排入 `heroRowPending`，等**本局連爆全部結束後**才發（與 v3 的 JP 同一個時機，
避免報獎蓋掉連爆演繹）。合成狀態一直留在盤面上（永遠不會 isMatched，所以不會被消除），
其他列照常連爆，下一次 spin 重鋪盤面時才消失。

角色列**不動 `splitCount`、也不寫進 `synthesizedColors`** —— 它是給分／發獎事件，
不是一次合成。分裂的來源只有一般合成（第一刀）與進階合成（第二刀）。

五個實作細節：

1. 偵測掛在 `STATE.SYNTHESIS` 的初始化，且排在一般合成掃描**之前**——
   否則那一列會先被當成一般合成候選。角色列不可能含船頭船尾，與船的偵測互不干擾。
2. 一次只處理由上往下第一個成立的列；合成後那一列不再是角色列，不會重複計。
3. 合成格「代表哪幾色」原本是 `[colorId, colorId2]` 硬組的。改成統一問
   `mergeColorIds(cell)`，它對帶 `isHeroRow` 的格**回空陣列** ——
   `calcWin()` 的兩處 `includes()` 因此都不會命中，整列在計分上等於不存在。
4. `computeCurrentWays()`（畫面左側的 WAYS 數字）**也必須另外擋一次**，讓角色列只算
   中性的 1。它走的是 `cell.colorId` 而不是 `mergeColorIds()`，所以 (3) 擋不到它 ——
   漏掉的話會出現「WAYS 顯示 7776、實際只賠 1024」這種兩邊對不上的狀況（單元測試
   `hero.noWaysContribution` 就是在守這個）。
5. 格子上仍留著 `colorId`／`colorIds`，那是給 `drawHeroRowCell()` 畫五色漸層用的，
   **不可拿來計分**。

## 四、出牌規則

- **五色合成率相等**：五色同權重，角色 7／武器 12（NG 另加船頭船尾各 2）。
- **第 3 輪單獨配重**（`V4_REEL3_CHAR_W`）：第 3 輪正好是角色列的中位格，
  在該欄把角色權重改成 海克力斯 3／伊阿宋 4／美狄亞 7／奧菲斯 9／阿塔蘭忒 13，
  做出「宙斯 < 赫拉 < MAJOR < MINOR < MINI」的五階獎頻率階梯。
  其他四輪維持等權，所以「五色合成率相等」不受影響（合成看的是同列相鄰兩格，不是單一輪）。
- `randomSymId()` / `randomReelSymId()` / `randomNormalSymId()` 都多收一個 `col` 參數；
  補牌（`applyGravity`）與初始鋪盤都要把欄號傳進去，否則第 3 輪的配重不會生效。

## 五、NG：地圖整段移除

`hasMapQuest()`（＝`!isV4()`）是總開關，擋掉 `drawMapPanel` / `drawMapShip` / `drawTMAnim` /
`tryAdvanceQuest` / `requestQuestAdvance` 的入列。盤面倍數恆 1，得分公式維持
`押注額 × 賠率 × Ways`。

盤面改為置中放大：`V4_LAYOUT_NG` grid x310 y124、格子 132（660×528）。
y 不能再往上：上方石像組下緣在 y=120；下緣也不能超過 spin 按鈕（y≈658）。

石像（`drawGodStatues`）在 v4 保留——它是兩尊神 JP 的顯示位置——但牌子下方的
v3 收集羅盤換成「角色列中心＝海克力斯／伊阿宋」的條件提示（v4 沒有累積過程）。

## 六、FG 航海地圖

新機制，與 v1～v3 的關卡地圖無關（另一組函式 `drawV4Voyage` 等）。
規則來自企劃 2026-08-13，呈現來自 2026-08-14 的美術需求。

**共 6 站**，倍數 ×1／×2／×3／×4／×5／×10。

- `v4VoyageStep` ＝ 船目前所在的站（0-based）。本次連爆得分套「船現在這一站」的倍數，
  **給完分船才前進 1 站** —— 所以第 1 次得分 ×1、第 2 次 ×2 …… 第 6 次 ×10。
- 船到第 6 站後不再前進，**本局後續連爆一律維持 ×10**。
- 站點狀態：`index <= v4VoyageStep` ＝ 已通過（彩色＋插旗）；其餘壓黑。**沒有雲霧遮罩**（見下）。
- **FG 每局重置**回起點；**SFG 不重置**，船位與倍數跨局保留。
- JP 與角色列彩金**不**與這個倍數相乘。

### 演繹（全部與連爆並行，不擋結算流程）

美術需求明訂「與盤面連爆並行，不打斷節奏」，所以 `v4VoyageAdvance()` 只是啟動動畫，
倍數當下就生效，**不等動畫播完**（這點與 v3 的羅盤不同 —— 那邊會等 `beaconAnim`）。
所有計時器由 `updateV4Voyage()` 每幀推進。

| 演繹 | 觸發 | 做法 |
|---|---|---|
| 船待機 | 平時 | 隨海浪上下晃＋輕微搖擺（sin LOOP） |
| 船移動 | 每段連爆得分 | 前半：原地彈起並淡出；後半：下一站淡入落下＋水花粒子 |
| 插旗 | 船抵達站點（動畫過半時） | 站點由壓黑轉彩色，`drawMapFlag()` 淡入小旗（沿用既有樣式） |
| 金羊毛終點 | 抵達第 6 站 | 聖林＋金羊毛亮起，全圖金光（`v4FleeceAnim`，時長為插旗的兩倍） |

### 與美術需求不同的兩個地方

1. **不做雲霧遮罩**（企劃 2026-08-14 決定）。
   美術需求原本寫「未通過站點之雲霧遮罩，沿用既有樣式」，實作後發現它與同一份需求裡的
   「未通過站點壓黑」互相打架 —— 藏寶圖那片霧濃度 0.99 幾乎全遮，壓黑的站點會被一起吃掉，
   玩家看不到上面還有幾站、終點是金羊毛。中間試過把濃度降到 0.66 讓站點以剪影透出來，
   最後企劃決定**整個拿掉**：未通過只用壓黑表示，整條航線一眼看得到終點。
   v1～v3 藏寶圖的 `drawMapFog()` 不受影響，那邊仍照舊。
2. **船停靠終點站時整體上移 20px**。聖林／金羊毛佔掉了站點本身的位置，
   船停在同一點會疊成一團；上移後看起來像停在聖林前的岸邊。

### NG 期間

美術需求寫「不顯示（或壓黑待機，待與美術討論）」。**目前採「不顯示」**，
因為 v4 的 NG 盤面是置中放大的，右側沒有留位置給面板（見 §六之二）。
若之後決定要壓黑待機，NG 版面得改回 FG 那組座標，盤面就不能置中放大了 —— 這兩件事互斥。

### 作廢的舊需求

美術需求載明「原『JP 小島／羅盤／小船』需求全數作廢」。那些是 v2／v3 的東西，
v4 本來就不會用到；**程式碼保留不刪**，因為 v1～v3 仍要能切回去對照。

## 六之二、v4 的版面（2026-08-14 依實截重排）

`syncV4Layout()` 在 `update()` 每幀檢查模式，**只在 NG↔FG 真的切換時**套用一次
（每幀都套會蓋掉調校面板「佈局」分頁的手動拖動）。它一次搬五個 LAYOUT 鍵：
`grid`／`mapPanel`／`statueL`／`statueR`／`fgCounter`。

| | NG | FG／SFG |
|---|---|---|
| 格子 | 132（盤面 660×528） | 130（盤面 650×520） |
| 盤面 | x310 y124（置中，中心 640） | x230 y120（中心 555） |
| 航海地圖 | 不畫 | x910 y120 245×520（貼盤面右緣，間距 30，同高） |
| 石像 L／R | x288／x681 | x203／x596 |
| FG 局數徽章 | （不畫） | x162 y375 |
| 三階彩金列 `cashTiers` | 中心 (640, 69) | 中心 (555, 68) |
| 聽牌圓圈 `heroPips` | 中心 (640, 100) | 中心 (555, 98) |

四個硬條件：

1. 盤面 y 至少 122（石像組下緣 ≈120），下緣不能超過 spin 按鈕（y≈658）。
2. 盤面左邊要留得下 **WAYS 欄位**（錨在盤面上：中心＝`GRID_X-68`、寬 110）
   與 **FG 局數徽章**（`fgCounter.x` 是**中心**）。兩者要對齊同一直行、上下錯開。
3. 航海地圖**貼著**盤面右緣。
4. 石像組對齊盤面中心：組跨距固定 704、兩者間距固定 393 ⇒ `statueL.x = 盤面中心 − 352`。

註：左側原本的 JP 獎池面板與連線紀錄面板（`drawJPPanel`／`drawWinLog`）**已是死程式**，
沒有被 `draw()` 呼叫，所以左邊整條是空的，可以給 WAYS／局數徽章用。

**三個踩過的坑（都是截圖驗證抓到的，單元測試守不到）**：

- `applyRuleLayout()` 直接覆寫 `LAYOUT.grid` 與 `CELL_SIZE` 卻沒清掉 `_v4LayoutMode`，
  於是「在 FG 中切換規則版本」之後 `syncV4Layout()` 誤判模式沒變、不把 FG 版面補回來。
  修法：`applyRuleLayout()` 裡把 `_v4LayoutMode` 設回 null。
- **`fgCounter` 沿用 v3 的 x=190 會壓在盤面上**。那個值是配 `DEFAULT_LAYOUT` 的盤面
  x=258 調的，搬到 v4 FG 的盤面（原本 x=195）就重疊了。改成跟著盤面走。
- **航海地圖沿用 v3 的 x=1010 離盤面 165px**，看起來像兩塊沒關係的東西。改成貼齊。

- **`cashTiers`／`heroPips` 一開始漏了沒進 `syncV4Layout()` 的搬移清單**，
  x 寫死在 640（NG 盤面中心）。FG 盤面中心是 555、石像也跟著左移，
  兩排就整體偏右 85px（企劃實機截圖回報「FG 的有點跑掉」）。
  FG 盤面上緣是 120、比 NG 少 4px，所以 FG 那組再各往上 1~2px。

教訓：新版本沿用舊版本的 LAYOUT 座標時，要把**每一個**跟盤面有相對關係的元件重算一次
（WAYS／局數徽章／石像／面板／彩金列／聽牌圓圈），不能只搬 `grid`。
**新增任何錨在盤面上的元件時，第一件事就是把它加進 `syncV4Layout()` 的清單**，
並用一條同時檢查 NG／FG／SFG 三個模式的測試釘住（`layout.ng` / `layout.fg` / `layout.sfg`）。

## 七、其他配套

- `RULE_LAYOUT[4]`、`RULE_SUMMARY[4]`、`RULE_VERSIONS[4]`、`DEFAULT_RULE_VERSION = 4`
- `TUNE_VERSION` 6 → 7（版面預設值變了，舊 localStorage 必須作廢）
- 強開選單改為版本相關（`forceMenuOptions()`，繪製與點擊命中共用同一份，不再各寫一份）：
  v4 把「條件1～4／獲得JP」換成「角色列⚡宙斯／角色列👑赫拉／角色列彩金」
- 遊戲說明頁新增「v4 角色列」段落，賠率表補阿塔蘭忒／弓

## 八、驗證紀錄（2026-08-14）

環境：本機無 node，用系統 Chrome `--headless=new` 跑。
語法檢查走 `new Function(src)`；邏輯測試把測試腳本注入 index.html 副本，
**直接同步呼叫 `update(1/60)`**（不依賴 rAF，headless 下 rAF 幾乎不前進）。

- 單元＋整合測試 **40／40 PASS**，涵蓋：圖示表與 id 配置、紫色只在 v4 出現、
  五色等權、第 3 輪階梯、分裂上限 3、一刀／第二刀、`ways = 248,832`、
  角色列偵測（含重複角色／混入武器／v3 不觸發三個否定案例）、
  **1×5 不是 wild**（`mergeColorIds` 回空、不進中獎組、不進 WAYS 顯示）、
  五階獎金額、航海倍數階梯與 FG／SFG 重置差異、v1～v3 未被改壞。
- 實跑：v1／v2／v3／v4 各模式各 6 局，**全部回到 IDLE、零 runtime error**；
  改動前的備份檔跑同一份 harness 結果一致（確認不是回歸）。
- 截圖驗證三個場景：NG 置中盤面、角色列 1×5、FG 航海地圖。

## 九、已確認的取捨（2026-08-14 企劃回覆）

- **角色列不給分裂**，單純給分＋發獎。
- **版面**：NG 置中放大、進 FG 收回讓出右側給航海地圖 —— 維持這個做法。
- **美術先用代圖**：阿塔蘭忒／弓走程式備援（紫底＋emoji＋名字），角色列 1×5 用五隻
  原圖並排＋中間格金框。

## 十、待辦

- 美術補件：`ART.sym[8]`／`ART.sym[9]`（阿塔蘭忒／弓）、`ART.wild[4]`（紫色 1×2 合成）、
  含紫色的 `ART.adv[*_4]`（進階合成 1×4）。補進去之後備援畫法會自動讓位，不需改程式。
- 紫色 `#a855f7` 與船頭船尾的 `#7c3aed` 同屬紫系，小圖示上可能不好分辨，
  美術進來後再看要不要把船改成別的色階。
- **第 3 輪權重（3／4／7／9／13）是初值**，五階獎的實際觸發率還沒做長跑統計。
  要比照 `sim-v3-core.js` 出一版 v4 純函式模擬器跑過再校準。

## 十一、對齊 2026-08-17 更新美術需求（亞格號_260817_更新美術需求.xlsx）

逐項比對後修正的九項：

| # | 項目 | 改成 |
|---|---|---|
| 1 | 彩金倍率 | MAJOR **100×**／MINOR **25×**／MINI **10×**（原 75／10／5）；配色改為各自中位角色的主色（紫／藍／綠） |
| 2 | 賠率表 | 全面換成「賠率表」分頁的值。五連線：伊阿宋 2、海克力斯 1.8、美狄亞 1.6、奧菲斯 1.4、阿塔蘭忒 1.2、船錨 1、木棒 0.8、魔法秘典 0.6、七弦琴 0.4、弓箭 0.2；四連＝÷2、三連＝÷4 |
| 3 | 顏色 | 美狄亞 藍→**紫**、奧菲斯 綠→**藍**、阿塔蘭忒 紫→**綠**（伊阿宋金、海克力斯紅不變） |
| 4 | 名稱 | 魔法→**魔法秘典**、弓→**弓箭**、刀盾→**船錨** |
| 5 | FG 帶入分裂 | `_resetForFreeGame()` 在 v4 **不歸零** `splitCount`，觸發 FG 那一局切的刀數帶進 FG |
| 7 | 相同獎項每局 1 次 | `heroRowPending` 以中位角色 id 去重 |
| 9 | FG 盤面銜接 | 進 FG／SFG 不再立刻 `startSpin()`，改設 `autoSpinDelay`，讓 NG 最後盤面現形後才由自動轉開第一局 |
| 34 | JP 合成演繹 | 新增（見下） |

### JP 合成演繹（美術需求編號 34）

五角相鄰成立時，`applyHeroRow()` 設 `v4HeroFlash = TF(TIMING.synthJpFlash)`（0.8s）
與 `v4HeroFlashColor = COLOR_HEX[中位角色色]`；`drawHeroRowCell()` 末段畫整格閃光＋
雙層外框光暈，節奏比照進階合成演繹，差別在顏色是該 JP 的色而非白金。
新增可調參數 `TIMING.synthJpFlash`。

### 兩項**沒有**照文件改，原因如下

- **#6 五角連線判定**（文件：「5 輪中各出現 1 個且**彼此相鄰**」）：維持現行的「同一整列」。
  理由是**文件自己矛盾** —— 美術需求編號 25–29 明確要求合體後是「圖示尺寸 **1×5**」，
  鋸齒狀的五格畫不成 1×5。且企劃口頭確認過是「5 隻角色左右相鄰成 1 整列」。
  待企劃裁示；若確定要放寬成鋸齒，合體的呈現方式要一併重新定義。
- **#8 已分裂圖示再合成＝2×2**：經企劃 2026-08-18 確認**維持現行畫法**
  （合成圖整張顯示＋右上角 ×N 徽章），不依刀數分層。

### 連帶影響：既有圖檔底版顏色對不上

顏色改動後，**美狄亞（現有圖是藍底）與奧菲斯（綠底）的圖檔與程式色不一致**，
盤面上會看到「綠底奧菲斯配藍色分裂徽章」這種狀況。需請美術依新色重出：
美狄亞／魔法秘典＝紫、奧菲斯／七弦琴＝藍、阿塔蘭忒／弓箭＝綠。

### 文件本身待釐清（未動程式）

1. 伊阿宋的武器：「一般遊戲」寫**刀盾**，「賠率表」與「美術需求說明」寫**船錨**（程式先採船錨）。
2. NG 最大連線數：「一般遊戲」D22 寫 32,768（＝只切 1 刀），但同頁 E43 說進階合成可切到 3×1。
3. 美術需求每個圖示都列「1×1、2×1、3×1、**4×1**」，但分裂上限已改 3×1，4×1 應作廢。
4. 圖示編號兩套：美術需求 0–4 角色／5–9 武器；賠率表 0–3 角色、8 阿塔蘭忒、4–7 武器、9 弓。
5. 「視覺流程圖」分頁自述「本頁分鏡待依 V6 重繪（待企劃）」，內容仍是舊的地圖／小島。
6. 「說明文件」分頁整頁仍是別款遊戲的內容（海龍王／RESPIN／輪盤），未更新。

## 十二、五角連線聽牌提示（企劃 2026-08-19）

上方兩塊彩金牌下方一排 **5 個圓圈**，對應 5 個輪。目的是把「還差幾個」可視化。

- **觸發**：某一列湊到 `V4_HINT_MIN`（＝3）個以上、**互不相同**的角色。
  重複角色不算進度（同一隻出現兩次湊不成五角連線）。
- **湊滿 5 個也要顯示**（`complete`，企劃 2026-08-19）。這裡有個坑：五角連線一成立
  `applyHeroRow()` 就把那五格合成成 1×5、變成 `isMerged`，逐格掃描再也掃不到角色 ——
  圓圈會剛好在「玩家達成的那一刻」整排熄掉。所以 `findHeroHints()` 會先從
  `mergedCells` 裡把帶 `isHeroRow` 的列撈出來補成 complete 狀態（用合成時存下的 `ids`）。
- **排序**：**已達成的列排最前面**，同一類之內才照列序「越上面的列越優先」。
  純列序的話會出現「玩家剛達成、圓圈卻在報上面某列的進度」，違背顯示達成的本意。
- 達成時：5 個圓圈全亮＋強光暈，左邊寫「達成！」，右邊直接寫拿到哪個獎
  （比輪播頁碼有用），對應的石像／彩金格照樣亮起。
- **呈現**：有角色的輪填入該角色圖示（裁成圓形）＋該色底與呼吸光；缺的輪是虛線空槽。
  只缺 1 個（`nearMiss`）時光暈與外框加強。
- **定獎預告**：中位（第 3 輪）已有角色時，同步亮起對應獎項 ——
  海克力斯→宙斯石像、伊阿宋→赫拉石像（走 `drawGodStatues` 原本的 `chosen` 呼吸光路徑），
  美狄亞／奧菲斯／阿塔蘭忒→三階彩金列對應那一格。中位是空槽時不亮任何獎項。
- **多列輪播**：淡入淡出，每列停留 `V4_HINT_HOLD`（2.2s）、淡入淡出 `V4_HINT_FADE`（0.35s）。
  **順序＝越上面的列越優先**（`findHeroHints()` 由上往下掃，陣列順序就是優先序），
  一開始一定從最上面那一列開始報，之後依列序往下輪、繞回最上面。
- **輪播狀態用「列號」而不是陣列索引**（`v4HintRow`）。滾輪逐格落定時清單幾乎每幀都在變，
  用索引會被一直重設、alpha 永遠停在淡入初期看不見；用列號則「只要這一列還符合就繼續報」。
- **轉動中的格算「未知」而不是「沒有」**（`v4CellSettled()`）：不拿來湊進度（避免提前洩題），
  但會被列入 `missing` → 盤面預報框畫在它上面。這樣「缺的那一格正在重轉」時就有預報感。

### 盤面預報框（`drawV4HintFrame`）

把目前預報那一列**缺的格**在盤面上框起來，畫在 `drawBoard()` 之後（蓋在圖示上）。
只要該列還符合條件就一直在，所以自然涵蓋企劃點名的三種時機：
**滾輪剛停**、**連爆單格重轉**、**缺的那一格正在重轉**。

| 狀態 | 盤面那一格 | 上方對應的圓圈 |
|---|---|---|
| 只缺 1 個（聽牌） | **金色強框**（4px＋強光暈＋四角標記） | **金色圈＋脈動外環＋「?」** |
| 還差 2 個以上 | 青色弱框（2.5px＋弱光暈） | 青色圈（弱光暈） |
| 完全沒有進度 | — | 淡虛線圈（待機） |

**盤面與圓圈用同一套顏色語言**（企劃 2026-08-19）：玩家要能把「上面這個圈」
直接對上「盤面那一格」，所以兩邊的強／弱框同色同步，不各自用一套配色。

### 圓圈圖示的浮現演繹（水晶球顯現）

企劃 2026-08-19：圖示不要突然出現，要有淡入、像水晶球顯現的感覺。
`V4_PIP_REVEAL`（0.45s），狀態在 `v4PipReveal[]` / `v4PipShown[]`，由
`updateV4PipReveal()` 每幀推進。某個圓圈從「空」變成「有角色」、或換成別的角色時重新起跳；
同一個角色持續存在不會重複起跳（否則會一直閃）。

四層疊起來做出「凝聚」感（`ease = 1 - rv³`，ease-out）：

1. 底色圓與圖示的透明度隨 ease 由 0 → 1
2. 圖示由 **0.78 倍放大**到 1 倍（在圓形裁切內縮放，所以是「從裡面長出來」）
3. 浮現初期蓋一層**由中心散開的白霧**（radial gradient），濃度隨 rv 遞減
4. 一圈**白光外環**從圓周向外擴散並淡出
5. 光暈（shadowBlur）在浮現當下加大 26，再收束回待機值

換列輪播時整排 id 都變 → 整排一起浮現，正好是「水晶球換一個畫面」的感覺。

### 預報框配色加強（2026-08-24）

企劃回饋：原本的預報框顏色（`#38bdf8` 藍／`#fde68a` 金）太淡不夠明顯。
新增專用常數 `V4_HINT_WEAK_COL`（`#00e5ff`，還差 2 個以上）／
`V4_HINT_STRONG_COL`（`#ffc93c`，聽牌只差 1 個），只用在預報框（盤面缺格框、
上方圓圈缺格框、圓圈達成時的外圈），不動遊戲其他地方沿用舊色的 UI（如 JP 文字、
彩金列光暈）。同時把線寬與光暈都加大，並在盤面框與圓圈框都墊一層深色底環／內描邊，
讓彩色框在任何底色上都不會被糊成一片光暈。

### 預報框改為「只在轉動時亮」＋落定特效（2026-08-24）

企劃回饋：盤面的預報框不應該一直亮著——只有「單格重轉」或「滾輪還在轉」時，
缺一個或缺兩個才要亮；而且缺一個／缺兩個都是金光，差別只在強弱（不再用藍色）。
滾輪放慢、最後轉到聽牌需要的角色時，那一格要有落定特效。

- `drawV4HintFrame()` 改成只畫在 `h.spinning`（還在轉的缺格）上，不再畫在
  `h.missing`（含已落定但非角色的格）上——一旦這一列的缺格全部落定，框就消失，
  這局對這一列已經是定局。
- 缺一個（聽牌）＝強金光；缺兩個＝同一個金色，只是線寬／光暈用 `inten=0.6`
  調弱。不再用藍色（`V4_HINT_WEAK_COL` 現在只留給上方圓圈的弱框繼續用，
  兩者職責不同：圓圈是不受轉動影響的常駐進度讀數，盤面框才是轉動中的預報）。
- 新增落定特效 `updateV4LandFlash()` / `drawV4LandFlash()`：用 `v4PrevSettled[c][r]`
  記錄上一幀是否已落定，抓「剛落定」那一幀；落定當下用 `v4RowIdsExcept(r, c)`
  算出「這一列除了這格之外」已落定的角色，如果那些角色已經 ≥3 個互不相同
  （代表落定前這格正被預報框盯著）且這次轉出的是新角色（非重複、非武器），
  就判定為「轉到需要的」，觸發 0.5 秒的金色爆閃＋白色擴散光環＋中心閃光。
  獨立於目前顯示哪一列的輪播，只要倒數還沒歸零就會畫。

驗證：新增 `landtest.js` 8 條測試（框只在轉動時顯示、落定特效在幫上忙／
重複／武器／未達門檻時的觸發與否、特效會自然倒數到 0、開場不誤觸發），
跟原本 27 條一起跑 35/35 全過。截圖驗證三種情境（缺一強金框、缺兩弱金框、
落定特效）目視確認符合預期。

### 聽牌中的滾輪逐步放慢（2026-08-24）

企劃回饋：盤面有差一個或差兩個的預報框時，滾輪速度要逐步放慢，不要跟平常一樣快。

新增 `v4AnticipationDecel(c, r)`：這一格是不是「正被聽牌預報盯著」（其餘 4 格
已落定、互不相同、數量 ≥ V4_HINT_MIN）；是的話回傳減速倍率——缺 1 個（聽牌）
放最慢，缺 2 個次之；都不是就回傳 `null`，照原本的 0.82 停。兩處滾輪停止邏輯
（初始轉動／單格重轉共用同一顆常數）都改成
`reelSpeed[c][r] *= v4AnticipationDecel(c, r) ?? 0.82`。

**倍率兩度調校**（換算成從全速 24px/幀衰減到停止門檻 1.8px/幀要花幾幀，
每幀衰減 `factor` 倍，`factor` 越接近 1 停得越慢）：

| 版本 | 缺 1 個（強） | 缺 2 個（弱） | 缺 1 個耗幀 | 缺 2 個耗幀 |
|---|---|---|---|---|
| 第一版（太快，企劃打回） | 0.95 | 0.9 | ~51 幀（0.85s） | ~25 幀（0.42s） |
| 第二版（企劃：再更慢兩倍） | **0.974** | **0.949** | ~100 幀（1.67s） | ~50 幀（0.83s） |

耗幀數是「從進入 stopping 那一刻就恆為聽牌狀態」的理論值；實際遊戲中
聽牌條件常常是滾輪停到一半才成立（例如缺 2 個的那兩輪不一定同時停），
所以實測的減速時間會是「一般速度＋減速」混合，比理論值短，但仍明顯
比沒有聽牌時的 ~14 幀長很多（實測樣本：21～41 幀），肉眼可見從模糊轉動
放慢到能看清楚正在跨過哪一格圖示的程度。企劃如果還嫌不夠慢，兩個
factor 可以再往 1 靠近（例如 0.974→0.985 約再翻倍）。

「聽牌角色列」強開（下方 §）刻意把留白格限制在第 3～5 輪，讓初始轉動也能
穩定示範這個效果——若留白格落在第 1／2 輪，它停下來時其餘 4 輪根本還沒轉完，
聽牌條件不成立，預報框／減速都不會出現。

驗證：`landtest.js` 新增 5 條（缺 1 強減速／缺 2 弱減速／未達門檻不減速／
重複不減速／v3 不啟用）。另外用真正的 `startSpin()`＋逐幀 `update()` 量測
40 次強開的落地耗時：一般欄恆為 13 幀；聽牌中的欄視強弱花 15 或 25 幀，
證實在完整流程裡真的變慢。

### 「聽牌（差 1 個）」強開（2026-08-24）

新增強開選單選項「角色列聽牌」（`hero_near`）：隨機在第 3～5 輪其中一輪留白，
其餘 4 輪放 4 隻不同角色；留白輪維持正常隨機轉出，可能剛好轉到缺的角色而
完成（連帶觸發落定特效／JP），也可能沒有——用來一鍵重現「差一個／差兩個」
情境，驗證預報框、滾輪減速、落定特效三個系統。

實作上有兩個容易忽略的陷阱，靠 30～150 次重複強開＋斷言才抓到：

1. **同色合成會吃掉強制角色**：留白格若剛好轉出「跟旁邊那隻強制角色同色的
   武器」，會被同色合成機制（相鄰、同色、一個角色一個武器）直接合成成 2×1，
   4 隻角色瞬間變 3 隻。修法：放置後檢查留白格與左右鄰居的 colorId，
   若衝突就重抽（僅限武器且同色的情況，其餘結果含真的轉出缺的角色維持原機率）。
2. **Ways 連線會吃掉強制角色**：4 隻強制角色若在盤面其他地方（任何一列）
   也出現，可能跟相鄰欄湊成一般 ways 連線，落地後被當中獎清掉、重力補新
   symbol——聽牌畫面還沒讓玩家看到就被吃掉了。修法：放置後掃描整盤，把這
   4 個 id 在其他地方的出現全部重抽掉，讓它們在整盤只出現這一次（第 0 列
   本身，含留白格，不受影響——留白格是否巧合重複或完成都是合法結果）。

驗證：`forcetest.js`（透過真正的 `startSpin()`＋逐幀 `update()` 而非只呼叫
覆寫函式）：選單含這個選項、150 次強開都恆為 4 隻互不相同角色、留白輪的
位置確實會變、150 次裡約 5 分之 4 是真的近乎完成（因為只留 1 種角色能完成，
機率上更常見的是沒完成、維持聽牌），完成時能正確被 `findHeroRow()` 合成。

`missing` 同時包含「還在轉的格」與「已落定但不是角色的格」——後者在連爆遊戲裡仍有意義
（那一格被消除重轉就有機會補上），所以一樣框起來。

### 連帶新增：三階彩金列

demo 原本沒有 MAJOR／MINOR／MINI 的顯示（`drawJPPanel` 是死程式），
所以「中位是美狄亞→亮 MAJOR」沒有東西可亮。新增 `drawV4CashTiers()`，
放在兩塊彩金牌下方（`LAYOUT.cashTiers`），數字＝倍率 × 押注額。
美術需求編號 41~43 的專屬報獎版之前，先用程式版。

### 版位（NG）

| 元件 | 位置 |
|---|---|
| 彩金牌下緣 | y 55 |
| 三階彩金列 `cashTiers` | 中心 (640, 69) → y 58~80 |
| 聽牌圓圈 `heroPips` | 中心 (640, 100) → y 83~117，寬 206（5×R17＋間距9） |
| 盤面上緣 | y 124 |

兩個都登錄在 `LAYOUT_META`，可在調校面板「佈局」分頁拖動。

**踩過的坑**：進度文字原本放在圓圈**下方**（y=127），直接壓在盤面框上 ——
下方到盤面上緣只剩 7px。改放到圓圈左右兩側（左＝「再 1 個！／差 N 個」、右＝「N/M 列」）。
這是實截才看出來的，座標算式本身沒有錯。

### 待調

`V4_HINT_MIN = 3` 是照企劃「3 個以上」設的。NG 角色出現率約 35%／格，
一列湊到 3 個不同角色並不罕見，實測常有 2~4 列同時符合、輪播會一直在動。
若覺得太吵，把這個常數改成 4（只在「差一個」時才提示）即可。

